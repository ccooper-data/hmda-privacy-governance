[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "hmda-privacy-governance"
version = "0.1.0"
description = "Privacy-safe HMDA disclosure-risk and analytical-utility platform"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [{name = "Cory Cooper"}]
dependencies = [
  "numpy>=1.26,<3",
  "pandas>=2.2,<3",
  "PyYAML>=6,<7",
  "requests>=2.32,<3",
]

[project.optional-dependencies]
dev = ["pytest>=8,<9", "pytest-cov>=5,<6", "ruff>=0.6,<1"]
warehouse = ["dbt-core>=1.8,<2", "dbt-duckdb>=1.8,<2", "duckdb>=1.1,<2"]

[project.scripts]
hmda = "hmda_privacy.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/hmda_privacy"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py311"
